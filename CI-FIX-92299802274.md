# Correção de CI — run 92299802274

## Diagnóstico

O workflow **Check Code Quality / Canonical CI Core** não falhou no build das imagens.
O job parou em `npm run lint:check` com 19 erros de lint/formatação.

Os builds GHCR de API e Manager em amd64/arm64 concluíram com sucesso no mesmo estado do código.

## Segurança da alteração em Baileys

A implementação Zapo não precisa reescrever o provider Baileys.

Nesta revisão, `whatsapp.baileys.service.ts` foi reduzido ao mínimo necessário:

1. remoção do bridge legado WavoIP (`useVoiceCallsBaileys` / `wavoipToken`);
2. anotação de tipo em `fetchAllGroups()` para satisfazer o TypeScript no build da branch com Zapo.

O comportamento de `fetchAllGroups()` foi mantido:
- mesma ordem dos grupos;
- mesmas propriedades;
- `participants` continua aparecendo somente quando `getParticipants == 'true'`;
- nenhuma chamada ou regra de sessão Baileys foi substituída por Zapo.

Métodos auxiliares adicionados anteriormente a Baileys (`isRegistered` e `requestPairingCode`) foram removidos desta revisão porque o `InstanceController` já possui fallback compatível. Isso reduz a superfície de alteração sobre o provider estável.

## Erros do lint corrigidos

### `src/api/controllers/call.controller.ts`
- formatação Prettier da exceção de operação de chamada não suportada.

### `src/api/integrations/channel/whatsapp/whatsapp.baileys.service.ts`
- remoção de linha em branco excedente;
- ajuste de tipagem em `fetchAllGroups()` mantendo comportamento original.

### `src/api/integrations/channel/whatsapp/zapo.whatsapp.service.ts`
- remoção de `InstanceDto` não utilizado;
- remoção de imports CommonJS via `require()`;
- uso de imports ESM/dinâmicos compatíveis com as regras do projeto;
- correções Prettier em áudio, sticker, configuração VoIP, persistência de mensagens, contexto citado e stubs;
- Zapo/VoIP continuam carregados de forma lazy para não interferir no startup de Baileys/Meta.

### `src/api/services/channel.service.ts`
- remoção de linha em branco excedente.

## Validações executadas

- `npm run test:zapo`: OK.
- Parser TypeScript: 203 arquivos `.ts` do core, 0 erros sintáticos.
- JSON: 0 arquivos inválidos.
- YAML: 35 arquivos válidos.
- Manager:
  - `npm run check`: 17 módulos OK;
  - `npm run build`: OK;
  - `npm run smoke`: 100 chamadas de contrato OK;
  - `npm run runtime:smoke`: OK.
- Importação do arquivo Zapo ordenada conforme `simple-import-sort`.
- Sem `require()` no provider Zapo.
- Sem `InstanceDto` não utilizado no provider Zapo.
- Sem reintrodução de `platform/`.
- `npm run test:zapo`: invariantes do provider aprovadas.

## Observações

Os avisos de Node.js 20 mostrados pelos actions Docker são warnings de runtime das próprias GitHub Actions e não causaram a falha do workflow.

O `npm audit` exibiu vulnerabilidades transitivas já presentes na árvore. Elas não foram alteradas com `npm audit fix --force`, pois isso poderia introduzir breaking changes fora do escopo desta correção.
