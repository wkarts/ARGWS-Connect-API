name: CI Core Canonical

on:
  workflow_call:

permissions:
  contents: read

jobs:
  validate:
    name: Canonical Validation
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Run canonical CI core
        uses: ./.github/actions/ci-core
