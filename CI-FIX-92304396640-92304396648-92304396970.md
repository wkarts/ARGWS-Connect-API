# Correção cirúrgica de CI — runs 92304396640 / 92304396648 / 92304396970

## Regra desta correção

Não reabrir problemas já resolvidos. Esta revisão altera somente os pontos explicitamente reportados nos três logs.

## Diagnóstico consolidado

### Canonical CI Core — run 92304396970

O `lint:check` falhou com quatro erros `prettier/prettier`:

1. `whatsapp.baileys.service.ts`: quebra de linha no cast de `groupFetchAllParticipating()`.
2. `zapo.whatsapp.service.ts`: formatação do `resolveMediaInput()` de áudio.
3. `zapo.whatsapp.service.ts`: fechamento/indentação da mesma expressão de áudio.
4. `zapo.whatsapp.service.ts`: formatação de `ZAPO_VOIP_MAX_CONCURRENT_CALLS`.

Nenhuma falha funcional de Baileys, QR ou pairing-code foi registrada.

### Database Integrity — run 92304396640

- PostgreSQL schema: OK.
- PostgreSQL migrations em banco vazio: OK.
- MySQL schema: OK.
- MySQL migrations em banco vazio: OK.
- PgBouncer parity: OK.
- PostgreSQL e MySQL falharam somente no mesmo type-check:

```text
zapo.whatsapp.service.ts: Argument of type 'string' is not assignable to parameter of type 'LogLevel'.
```

### GHCR Develop — run 92304396648

- Manager amd64: OK.
- Manager arm64: OK.
- API amd64/arm64: falharam somente no `npm run build` pelo mesmo `TS2345` de `LogLevel`.

## Correções aplicadas

### Baileys

Preservada integralmente a correção de tipagem já existente em `fetchAllGroups()`.

Foi alterada **somente a formatação** do `Object.values(...)` conforme o Prettier do CI. Não houve mudança em:

- QR code;
- código de pareamento;
- `protocolAuthMode`;
- fingerprint `Ubuntu / Chrome / 20.0.04` usado no pairing-code;
- identidade personalizada do QR;
- sockets/reconnect;
- mensagens, grupos ou sessões.

### Zapo

- Corrigida somente a formatação indicada pelo Prettier para áudio e `maxConcurrentCalls`.
- `ZAPO_LOG_LEVEL` agora é validado em uma união local compatível com o `LogLevel` oficial do Zapo:
  - `trace`
  - `debug`
  - `info`
  - `warn`
  - `error`
- Valores inválidos ou ausentes usam `warn`.

Isso remove o `TS2345` sem cast inseguro de string arbitrária.

## Validação

- TypeScript parse: 204 arquivos OK.
- `Zapo LogLevel` boundary: OK.
- Zapo provider invariants: OK.
- Manager check: 17 módulos OK.
- Manager build: OK.
- Manager smoke: 100 chamadas OK.
- Manager runtime smoke: OK.
- `npm ci --offline --dry-run --ignore-scripts`: 1.228 pacotes resolvidos.
- Comparação contra o pacote anterior: somente dois arquivos de runtime alterados.

## Arquivos de runtime alterados

```text
src/api/integrations/channel/whatsapp/whatsapp.baileys.service.ts
src/api/integrations/channel/whatsapp/zapo.whatsapp.service.ts
```
