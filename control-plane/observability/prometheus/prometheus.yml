global:
  scrape_interval: 15s
scrape_configs:
  - job_name: control-plane
    static_configs:
      - targets: ["api:8090"]
  - job_name: dynamic-platform-targets
    file_sd_configs:
      - files: ["/etc/prometheus/targets/*.json"]
        refresh_interval: 30s
