name: ${COMPOSE_PROJECT_NAME:-argws-connect-api-dev}

services:
  api-argws-connect-api-dev:
    container_name: api-argws-connect-api-dev
    image: ${ARGWS_CONNECT_API_IMAGE:-ghcr.io/wkarts/argws-connect-api:develop}
    pull_policy: always
    restart: unless-stopped
    ports:
      - "${ARGWS_CONNECT_BIND_ADDRESS:-127.0.0.1}:${ARGWS_CONNECT_API_HOST_PORT:-8080}:8080"
    volumes:
      - ${ARGWS_CONNECT_INSTANCES_DATA_PATH:-./volumes/instances}:/argws-connect/instances
    networks:
      - argws-connect-api-dev-net
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 10
      start_period: 60s

networks:
  argws-connect-api-dev-net:
    name: ${ARGWS_CONNECT_NETWORK_NAME:-argws-connect-api-dev-net}
    driver: bridge
