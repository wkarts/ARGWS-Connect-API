# Referência histórica — domínio financeiro

Estas páginas vieram do projeto-base financeiro e foram removidas do `frontend/src` ativo da **Connect|API Platform**.

Elas não entram no build, não aparecem no router e não representam a identidade ou o domínio atual do produto. Foram preservadas apenas como referência de implementação para fluxos complexos do Control Plane/tenant.

Não mova estes arquivos novamente para `frontend/src` por renomeação automática. Qualquer funcionalidade equivalente na Connect|API deve ser modelada no domínio de comunicação e integração.
